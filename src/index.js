import React from "react";
import ReactDOM from "react-dom";
import CVBuilder from "./CVBuilder/Bundler.js";

const root = ReactDOM.createRppt(document.getElementById('root'));
root.render(
    <React.StrictMode>
        <CVBuilder />
    </React.StrictMode>
);
